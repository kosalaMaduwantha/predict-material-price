"""
Main module for the Material Cost Forecaster application.
"""

# This is a minimal version just to make the import work
import dash
from dash import html

app = dash.Dash(__name__)
server = app.server

app.layout = html.Div([
    html.H1("Material Cost Forecaster")
])

if __name__ == '__main__':
    app.run_server(debug=True)
"""
